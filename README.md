# Prolog Verifiable Credential (VC) Tools

This NPM package will be used to make the translation between signed and verified VCs into the containing Prolog rules based on a specific content JSON schema.

**WIP**
