{
  "title": "File Resource",
  "type": "object",
  "properties": {
    "claimType": {
      "type": "string",
      "const": "file"
    },
    "resource_id": { "type": "string" },
    "updateView": {
      "type": "string",
      "enum": ["assert", "retract", "assertz", "asserta"]
    }
  },
  "required": ["claimType", "resource_id"]
}
