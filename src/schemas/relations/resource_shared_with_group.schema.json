{
  
  "title": "Resource Shared With Group",
  "type": "object",
  "properties": {
    "claimType": {
      "type": "string",
      "const": "resource_shared_with_group"
    },
    "resource_id": { "type": "string" },
    "group_id": { "type": "string" },
    "updateView": {
      "type": "string",
      "enum": ["assert", "retract"],
      "default": "assert"
    }
  },
  "required": ["claimType", "resource_id", "group_id", "updateView"]
}