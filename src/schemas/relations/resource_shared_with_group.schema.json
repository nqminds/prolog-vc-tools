{
  "title": "Resource Shared With Group",
  "type": "object",
  "properties": {
    "claimType": {
      "type": "string",
      "const": "resource_shared_with_group"
    },
    "sharer_id": { "type": "string" },
    "resource_id": { "type": "string" },
    "group_id": { "type": "string" },
    "updateView": {
      "type": "string",
      "enum": ["assert", "retract", "assertz", "asserta"]
    }
  },
  "required": ["claimType", "sharer_id", "resource_id", "group_id"]
}
