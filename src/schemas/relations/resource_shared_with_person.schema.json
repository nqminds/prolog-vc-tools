{
  "title": "Resource Shared With Person",
  "type": "object",
  "properties": {
    "claimType": {
      "type": "string",
      "const": "resource_shared_with_person"
    },
    "sharer_id": { "type": "string" },
    "resource_id": { "type": "string" },
    "person_id": { "type": "string" },
    "updateView": {
      "type": "string",
      "enum": ["assert", "retract", "assertz", "asserta"]
    }
  },
  "required": ["claimType", "sharer_id", "resource_id", "person_id"]
}
