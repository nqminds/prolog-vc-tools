{
  "title": "Resource Owned By Person",
  "type": "object",
  "properties": {
    "claimType": {
      "type": "string",
      "const": "resource_owned_by_person"
    },
    "resource_id": { "type": "string" },
    "person_id": { "type": "string" },
    "updateView": {
      "type": "string",
      "enum": ["assert", "retract", "assertz", "asserta"],
      "default": "assert"
    }
  },
  "required": ["claimType", "resource_id", "person_id", "updateView"]
}
