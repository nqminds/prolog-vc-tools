{
  "title": "Custom Rule",
  "type": "object",
  "properties": {
    "claimType": {
      "type": "string",
      "const": "rule_custom"
    },
    "prolog": { "type": "string", "description": "Prolog rule as a string" }
  },
  "required": ["claimType", "prolog"]
}
