{
  "name": "prolog-vc-tools",
  "version": "5.1.2",
  "description": "Provides helper tools for using VCs containing Prolog rules.",
  "license": "ISC",
  "author": "Henry Pearson <henry@nquiringminds.com> (https://nquiringminds.com)",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "build": "tsc",
    "ci": "npm run build && npm run format-check && npm run test",
    "format": "prettier --write .",
    "format-check": "prettier --check .",
    "dev": "vitest",
    "local-release": "changeset version && changeset publish",
    "prepublishOnly": "npm run ci"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "@types/node": "^24.5.2",
    "@vitest/coverage-v8": "^3.2.4",
    "prettier": "^3.6.2",
    "typedoc": "^0.28.14",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "swipl-wasm": "^5.1.5"
  }
}
